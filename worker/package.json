{
  "name": "souba-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "probe": "wrangler deploy && echo '部署完成，用 curl 打 /probe'"
  },
  "devDependencies": {
    "wrangler": "^4.126.0",
    "@cloudflare/workers-types": "^5.20260826.1",
    "typescript": "^7.0.2"
  }
}
